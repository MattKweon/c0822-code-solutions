/* exported isUpperCased */
/*
[x] determine of the input string contains all uppercased letters
[x] if so, return true
[x] if not, return false
*/

function isUpperCased(word) {
  var output = word === word.toUpperCase();
  return output;
}
